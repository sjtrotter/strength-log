package io.github.sjtrotter.strengthlog.domain

/**
 * Stands in for the real domain model until M1 lands (types, catalog seed,
 * goal math, cascade, generator, rotation — spec §3-§6).
 */
object Placeholder {
    const val MODULE_NAME: String = "domain"
}
